# 2026-03-03

- Prevented the technical-analysis background loop from booting in rake contexts (e.g., `db:migrate` on Render build).
- This avoids `UpdateTechnicalAnalysisJob` being enqueued during build-time tasks where Active Job async workers can outlive migration DB setup and hit `ActiveRecord::ConnectionNotDefined`.
