# 2026-03-12

## Order safety hardening

- Standardized order placement through `Orders::Gateway` as the only live broker placement path.
- Removed remaining call-site `PLACE_ORDER` branching in processors so the gateway is the single runtime gate.
- Updated operational docs (`README.md`, `CLAUDE.md`, `AGENTS.md`) and added `.env.example` with `PLACE_ORDER`.
